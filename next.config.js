/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdni.iconscout.com', 'raw.githubusercontent.com'],
  },
}

module.exports = nextConfig
